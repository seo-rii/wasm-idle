{
	"name": "@wasm-idle/runtime-js-sandbox",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"description": "QuickJS WebAssembly sandbox helpers for wasm-idle",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js",
			"default": "./dist/index.js"
		}
	},
	"files": [
		"dist"
	],
	"scripts": {
		"build": "rm -rf dist && tsc -p tsconfig.json",
		"check": "tsc -p tsconfig.json --noEmit"
	},
	"dependencies": {
		"quickjs-emscripten": "0.32.0"
	},
	"devDependencies": {
		"typescript": "^6.0.2"
	}
}
