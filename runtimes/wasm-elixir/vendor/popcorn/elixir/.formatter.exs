# Used by "mix format"
[
  inputs: [
    "{mix,.formatter}.exs",
    "{config,lib,patches}/**/*.{ex,exs}",
    "test/*.{ex,exs}",
    "test/{examples,popcorn,support}/**/*.{ex,exs}",
    "test/fixtures/*/*.{ex,exs}"
  ]
]
