{
	"name": "@wasm-idle/svelte",
	"version": "1.0.0-next.1",
	"description": "Svelte bindings for wasm-idle core",
	"license": "MIT",
	"type": "module",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js",
			"default": "./dist/index.js"
		}
	},
	"files": [
		"dist"
	],
	"publishConfig": {
		"access": "public",
		"tag": "next"
	},
	"peerDependencies": {
		"@wasm-idle/core": "1.0.0-next.1",
		"svelte": "^5.0.0"
	},
	"dependencies": {
		"@wasm-idle/core": "workspace:*"
	},
	"devDependencies": {
		"svelte": "^5.55.4",
		"typescript": "^6.0.2"
	},
	"scripts": {
		"build": "rm -rf dist && tsc -p tsconfig.json",
		"check": "tsc -p tsconfig.json --noEmit"
	}
}
