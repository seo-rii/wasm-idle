# wasm-idle Migration Notes

This is the migrated `../import-jungol` helper repository. It is included so Jungol import and upload
automation remains available from the monorepo alongside the runtime and judge toolchain code.

The migration audit checks tracked and non-generated source files against the sibling checkout when
that checkout is present.
